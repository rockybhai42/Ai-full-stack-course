module.exports = {
  testEnvironment: 'node',
  transform: {},
  testTimeout: 20000,
};

module.exports = {
  testEnvironment: 'node',
  transform: {},
  testTimeout: 20000,
  setupFiles: ['<rootDir>/tests/helpers/setupEnv.js'],
}